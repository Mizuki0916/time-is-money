# 使うプロバイダのキーだけ入れれば OK（config.json の llm.provider と合わせる）
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# 株価は Yahoo Finance（キー不要）を使います。
# 取れないときの控えとして stooq を使いたい場合だけ設定（任意）
# https://stooq.com/q/d/?s=spy.us&get_apikey
STOOQ_API_KEY=

# 自宅PCから直接 GitHub に push する場合のみ（任意）
GIT_AUTO_PUSH=0
